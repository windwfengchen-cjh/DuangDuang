# 2026-03-26 记忆

## 群内@无反应问题复盘

**问题**：用户在"需求调研-杨政航-0325"群(oc_59539a59e696491bd93241ecd9b8c80d)@我，但没有收到消息。

**原因**：群里的@消息被路由到了其他会话（子智能体会话），没有到达主会话。需求跟进流程启动后，我在主会话中创建了记录和调研群，但后续群内消息由子智能体处理。

**教训**：
1. 调研群创建后，群内消息由子智能体处理，不是主会话
2. 如果在调研群内@我，消息不会回到主会话
3. 需求跟进的完整流程在子智能体中执行

---

## 需求跟进触发条件确认

**触发条件必须同时满足**：
1. 引用一条消息
2. 说"跟进这个需求"（或类似触发词）
3. 在任意群（非调研群）中

**触发词列表**：
- "跟进这个需求"
- "跟进需求"  
- "记录这个需求"

**注意**：在调研群内说触发词不会重复创建需求，群内消息由子智能体监听处理。

---

## 两个维护中的在线表格

| 表名 | App Token | Table ID | 用途 |
|------|-----------|----------|------|
| **业务反馈问题记录表** | KNiibDP6KaRwopsPbRucr752ntg | tblyDHrGGTQTaex6 | 记录产研群问题反馈 |
| **需求跟进清单** | Op8WbbFewaq1tasfO8IcQkXmnFf | tbl0vJo8gPHIeZ9y | 需求跟进到PRD流程 |

**只有这两个表在维护**，没有其他在线表格。

---

## Bitable 物理删除记录方法

**脚本位置**：`/home/admin/openclaw/workspace/delete_requirement_records.py`

**使用方法**：
```bash
python3 delete_requirement_records.py
```

**实现原理**：
- 调用飞书开放平台 DELETE API：`https://open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records/{record_id}`
- 使用 tenant_access_token 认证（从 `~/.openclaw/openclaw.json` 读取凭证）
- 物理删除，不是更新状态

**注意**：`feishu_bitable_delete_record` API 不可用，必须通过 Python 脚本直接调用。

---

## 派子智能体规范更新

**群内问题反馈转发**：
- ❌ 错误：让子智能体自行选择工具（"使用 feishu_doc 或相关工具"）
- ✅ 正确：明确指定使用 `feishu-feedback-handler skill`

**子智能体异常处理**：
- 如果派子智能体报错"不可用"或"被禁止"，必须立即重新派一次
- 禁止：自己执行、告知用户等待配置

## 协调者违规案例（重要教训）

**违规时间**：2026-03-26 上午  
**违规场景**：调查"店铺端-技术沟通"群消息转发问题时

**❌ 错误做法**（我实际做的）：
```python
# 直接调用 exec 查询日志
exec({"command": "grep -E ... ~/.openclaw/agents/main/sessions/..."})
```

**✅ 正确做法**（应该做的）：
```python
# 派子智能体查询
sessions_spawn({
    "task": "查询会话日志，分析店铺端-技术沟通群的子智能体派发信息..."
})
```

**违规原因**：
- 惯性思维：看到查询任务下意识直接执行
- 任务误判：误以为"查日志"是读操作可以自己做
- 急于回复：想快速找到答案

**强化规则**：
> **任何任务都必须派子智能体，包括查询、调查、分析**  
> **没有例外！**  
> **我是协调者，我绝不执行！**

---

## requirement-follow skill 长时监听模式实现（2026-03-26 重要）

### 实现内容
- ✅ 新增 `runLongRunningListener()` 方法
- ✅ 子智能体超时支持 24 小时（86400秒）
- ✅ 每5分钟心跳，每5秒轮询消息
- ✅ 多调研群并发（每个群独立子智能体）

### 关键代码变更
**文件**：`~/.openclaw/skills/requirement-follow/src/index.ts`
```typescript
async runLongRunningListener(chatId: string, heartbeatIntervalMs = 5 * 60 * 1000): Promise<void> {
  // 24小时监听，直到调研完成或超时
}
```

### 使用方式
```typescript
sessions_spawn({
  task: "使用 requirement-follow skill 启动长时监听...",
  timeoutSeconds: 86400,  // 24小时
  label: "豆浆-需求跟进-长时监听",
  ...
});
```

### 重要提醒
**监听功能已完整实现**，不要再被旧信息误导：
- ✅ `createResearchSession()` - 已调用
- ✅ `startChatPolling()` - 已实现
- ✅ `handleResearchMessage()` - 已调用
- ✅ `runLongRunningListener()` - 已新增

---

## 今日任务完成情况

| 任务 | 状态 | 备注 |
|------|------|------|
| 周四安全检查 | ✅ 完成 | 32个skill审计，0危险标记 |
| 清空需求跟进清单 | ✅ 完成 | 物理删除1条记录(recveTqW7sJAgB) |
| 更新长期记忆规则 | ✅ 完成 | AGENTS.md、MEMORY.md 已更新 |
| 验证豆浆子智能体 | ✅ 完成 | sessions_spawn 正常工作 |
| **实现长时监听模式** | ✅ 完成 | requirement-follow skill 功能完整 |

**明日待办**：周五11:00每周总结心跳任务 (2026-03-27)

---

## 子智能体"豆浆"成功执行需求跟进（2026-03-26 11:10）

### 执行结果
- ✅ **需求记录**: recveWiqmST9XT
- ✅ **调研群**: 需求调研-杨政航-0326 (oc_3de70c1e5567aa290c923d01ceb3fb09)
- ✅ **需求内容**: 帮我对接保险系统，给保险用户办卡
- ✅ **需求方**: 杨政航

### 技术突破
**`sessions_spawn` 成功配置**:
```json
// 可行的调用方式
{
  "label": "豆浆-需求跟进",
  "mode": "run",
  "timeoutSeconds": 180
  // ❌ 不需要 runtime: "acp"
  // ❌ 不需要 agentId: "doujiang"
}
```

### 失败的尝试
1. ❌ `runtime: "acp"` + `agentId: "doujiang"` → acpx exited with code 1
   - 原因: ACPX 期望 JSON 格式消息，但 Gateway 输出文本日志导致解析失败
2. ❌ `runtime: "subagent"` + `agentId: "doujiang"` → agentId is not allowed
   - 原因: 需要在 agents.defaults.subagents.allowedAgents 中配置

### 已完成的配置
- ✅ 安装 acpx CLI: `npm install -g acpx@0.1.16`
- ✅ 配置 acpx 插件: `~/.openclaw/openclaw.json` plugins.entries.acpx.config.command
- ✅ 创建子智能体"豆浆": `openclaw agents add doujiang`

### 当前限制
- ACPX runtime 与 Gateway 日志格式不兼容（非 JSON 输出）
- 子智能体必须使用默认 subagent 模式（不带 runtime 参数）

---

## 会话重置记录（2026-03-26 14:40）

**上下文积累**: 105k/128k (82%)
- 上午会话因技术调试积累大量上下文
- 用户主动要求重置会话
- 使用 `/reset` 或调用 `session_status(model="default")` 重置
