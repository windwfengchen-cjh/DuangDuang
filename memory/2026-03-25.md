# 2026-03-25 工作记录

## 📝 重要认知固化

### 排查消息转发问题 - 先查日志
**来源**: Boss 陈俊洪 2026-03-25 11:52

**核心原则**: 遇到消息转发问题（如@人没高亮、消息格式错误等），**第一时间查日志**，而不是猜测或询问。

**日志路径**:
```
~/.openclaw/skills/feishu-feedback-handler/logs/forward_payloads_YYYY-MM-DD.jsonl
```

**排查步骤**:
1. **查日志** - 查看完整报文
   ```bash
   tail -n 100 ~/.openclaw/skills/feishu-feedback-handler/logs/forward_payloads_*.jsonl
   ```
2. **分析 payload** - 检查消息格式是否正确
3. **定位问题** - 是构造问题还是发送问题

**日志保留策略**: 自动保留1天，超过1天的旧日志自动清理

---

## 其他工作

### 已完成的任务
1. ✅ 修复时间戳问题 - 所有 `event.create_time` 改为 `Date.now()`
2. ✅ 修复需求跟进成员可见性问题 - 232043 错误处理
3. ✅ 添加转发日志记录功能
4. ✅ 添加日志自动清理功能（保留1天）
5. ✅ 硬删除需求跟进清单错误记录

### 已定位并记录的问题
- ✅ @人高亮问题已定位 - 子智能体直接发送文本消息使用了 `<at id="...">` 格式
- ✅ 已更新 MEMORY.md 记录正确的消息转发格式规范

---

## 🔧 问题定位：@人没高亮

**时间**: 2026-03-25 15:35
**问题**: 天猫号码排序反馈消息中 @人没高亮
**原始消息ID**: om_x100b530caeb104acc32745b5af486d9

### 问题根源
子智能体直接调用 `message` 工具发送文本消息，使用了错误的 @ 格式：
```json
{
  "message": "【产研反馈-Bug】...\n<at id=\"ou_...\">施嘉科</at> ..."
}
```

### 正确格式
必须使用 Post 富文本格式：
```json
{
  "msg_type": "post",
  "content": {
    "post": {
      "zh_cn": {
        "title": "【产研反馈-Bug】",
        "content": [
          [{"tag": "at", "user_id": "ou_...", "user_name": "施嘉科"}]
        ]
      }
    }
  }
}
```

### 后续行动
- 已更新 MEMORY.md 长期记忆
- 已记录子智能体任务规范
- 下次派子智能体时必须明确消息格式要求

## 年份确认
当前年份: **2026年**
任何显示 2024/2025 年的时间戳都是异常的。

---

## 💾 记忆固化确认（2026-03-25 15:51）

**操作**: Memory flush - 将关键认知固化到长期记忆

**已固化的内容**:
| 条目 | 位置 | 状态 |
|------|------|------|
| 消息转发格式规范 | MEMORY.md | ✅ 已更新 |
| @人高亮问题分析 | memory/2026-03-25.md | ✅ 已记录 |
| 子智能体任务规范模板 | MEMORY.md | ✅ 已固化 |

**关键记忆锚点**:
> "转发带@的消息 → Post格式 → `{tag:'at',user_id:'...',user_name:'...'}` → 禁止 `<at id=\"...\">`"

---

## 🔄 需求跟进流程规范更新（2026-03-25 16:14）

**Boss 指令**: "派子智能体使用 requirement_follow 时，需要让子智能体自动执行记录需求、自动创建调研群、启动问答+监听模式收集需求信息这几个步骤，整个过程需要连贯，不需要来跟我确认"

**更新内容**:
1. 需求跟进流程必须**连贯自动执行**，禁止中途询问确认
2. 子智能体一旦启动，自动完成：记录需求 → 创建调研群 → 启动监听 → 7个问题引导
3. 只有群内指令响应阶段才等待用户输入
4. 已更新 MEMORY.md 和 AGENTS.md 的任务描述模板

**关键记忆锚点**:
> "需求跟进 → 派子智能体 → 自动连贯执行 → 记录→建群→启动监听→7个问题 → 全程无需确认 → 最后统一汇报"

---

## 🤖 豆浆 - 需求跟进专员创建（2026-03-25 19:17）

**背景**: 解决每次派子智能体执行需求跟进效果不一致的问题

**身份定义**:
- **名字**: 豆浆
- **角色**: 常驻需求跟进专员
- **职责**: 只负责需求跟进任务，其他任务拒绝执行
- **使用方式**: 派子智能体时引用"豆浆"身份

**核心铁律**:
```
1. 只负责需求跟进任务，其他任务拒绝执行
2. 必须使用 requirement-follow skill，禁止绕过
3. 自动连贯执行：记录→建群→启动监听→7个问题
4. 全程无需确认，最后统一汇报
5. 禁止中途询问"是否要创建群"等问题
```

**执行步骤**:
1. 【自动】提取需求信息 → 检查重复 → 创建需求记录
2. 【自动】创建调研群（群名：需求调研-{需求方}-{日期}）
3. 【自动】发送欢迎消息，等待"开始调研"指令
4. 【等待指令】等待 Boss 发送"开始调研"
5. 【自动】启动问答+监听模式，发送第一个问题

**群内指令**:
- "开始调研" → 启动问答+监听模式
- "完成调研"/"生成PRD" → 结束调研，生成PRD
- "取消" → 终止调研

**记忆锚点**:
> "派豆浆 → 使用 requirement-follow skill → 自动连贯执行 → 记录→建群→等待开始调研→7个问题"

---

## 🔄 需求跟进工作流更新（2026-03-25 19:32）

**更新前**: 创建群后立即发送第一个问题
**更新后**: 创建群后发送欢迎消息，等待"开始调研"指令

**新的工作流程**:
```
Boss: "跟进这个需求" + 引用消息
    ↓
协调者派"豆浆"
    ↓
豆浆: 记录需求 → 创建调研群 → 发送欢迎消息
    ↓
等待"开始调研"指令
    ↓
Boss: "开始调研"
    ↓
豆浆: 启动问答+监听 → 7个问题 → 生成PRD
```

**已更新文件**:
- requirement-follow skill (`waiting_start` 状态)
- MEMORY.md (豆浆规范)
- AGENTS.md (流程文档)

---

## 📝 协调者派子智能体规范（2026-03-25 19:43）

**核心原则**: 派子智能体时，必须明确指定使用的工具、skill或脚本，禁止让子智能体自行选择处理方式。

**标准任务描述格式**:
```markdown
任务：[任务名称]

**执行方式（必须明确指定）**：
- 使用工具：[具体工具名]
- 或使用 skill：[具体skill名]
- 或使用脚本：[具体脚本路径]
- 禁止：直接构造 HTTP 请求、绕过skill直接操作

**任务详情**：
[具体内容]
```

**不同任务的指定方式**:
| 任务类型 | 指定方式 |
|---------|----------|
| 需求跟进 | 派"豆浆"，使用 requirement-follow skill |
| 查询表格 | 使用 feishu_bitable_* 工具 |
| 删除记录 | 调用 DELETE API 硬删除 |
| 发送消息 | 使用 message 工具 |

**记忆锚点**:
> "派子智能体 → 明确指定工具/skill/脚本 → 禁止自行选择 → 豆浆只负责需求跟进"

---

## 🔧 硬删除标准流程（2026-03-25 19:43）

**问题**: 子智能体多次使用清空字段的软删除，未执行真正的硬删除

**标准硬删除流程**:
```
1. 从 ~/.openclaw/openclaw.json 读取飞书凭证
2. 获取 tenant_access_token
3. 调用 DELETE API:
   DELETE https://open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records/{record_id}
4. 验证删除结果（查询返回 RecordIdNotFound）
```

**验证方式**: 删除后查询，应返回错误码 1254043 (RecordIdNotFound)

**记忆锚点**:
> "删除表格记录 → DELETE API → 验证 RecordIdNotFound → 禁止软删除"

---

## 🔧 修复 requirement-follow 导入路径（2026-03-25 19:05）

**问题**: feishu-feedback-handler 导入 requirement-follow 路径错误
- 原路径: `../../requirement-follow/dist/index.js` (错误)
- 正确路径: `requirement-follow` (通过 path mapping)

**修复方案**:
1. 修改 `src/index.ts` 导入语句
2. 添加 `tsconfig.json` path mapping
3. 创建 `node_modules/requirement-follow` 符号链接

**记忆锚点**:
> "skill 间导入 → 使用 path mapping → tsconfig paths + node_modules symlink"

---

*End of 2026-03-25 工作记录*
